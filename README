写吧，都可以写
这个项目代码写得比较草率，我自己跑了一遍就扔上来了，有什么bug可以跟我反馈，加分
某些文件需要你自己写，最烦人的包括dockercompose还有那个requirements.txt
你要实现：代码主体进一个叫backend的容器(通过dockerfile实现)，然后使用dockercompose一键部署一个容器网络，包括这个backend和一个mysql容器。到这里这个容器就该可以正常跑了，截一次图
然后再在外面建立一个nginx容器，挂进网络帮这个backend实现反向代理，这一步不进dockercompose，截一次图
最后修改代码，整一个数据卷，把报错日志挂在容器外面，截一次图

加分项，想办法弄个域名让我可以在公网访问到你的容器，私信敲我约时间
这题确实不难，属于最接近实际工作需求的一集，在容器里跑进程是好习惯
